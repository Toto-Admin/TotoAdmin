import { AgmPolygonDirective } from './agm-polygon.directive';

describe('AgmPolygonDirective', () => {
  it('should create an instance', () => {
    const directive = new AgmPolygonDirective();
    expect(directive).toBeTruthy();
  });
});
